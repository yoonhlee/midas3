services:
  jobsim:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.server
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./reports:/app/reports
    restart: unless-stopped
